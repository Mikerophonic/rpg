export default class Character {
    constructor(str, dex, int, def, mag) {
      this.str = str;
      this.dex = dex;
      this.int = int;
      this.def = def;
      this.mag = mag;
    }
  
    
  }